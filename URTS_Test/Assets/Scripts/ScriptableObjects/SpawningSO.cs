using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "Spawning Scriptable Object", menuName = "Scriptable Objects/Spawning Scriptable Object", order = 1)]
public class SpawningSO : MySO
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
